# ledger (testbed)

A tiny personal-finance ledger (modular layout: one module per CLI command in `ledger/commands/`, ledger file IO in `ledger/ledgerio.py`) used to benchmark fractal's parallel planner. Run tests with
`python -m pytest -q`.
